/**
 * Fase 18 — Twitter/X card reusa la misma OG image de marca (ver opengraph-image).
 * Next la registra como `twitter:image` por convención de archivo.
 */
export { default, size, contentType, alt } from './opengraph-image';
