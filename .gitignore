node_modules
.next
coverage/
out
build
.env*.local
# `.env*.local` NO cubre un `.env` pelado: si alguien crea ese archivo, se
# commitea. Este repo estuvo público, así que el margen de error era cero.
.env
.DS_Store
*.tsbuildinfo

# Claude Code worktrees (no versionar)
.claude/worktrees/

# Copias que crea macOS al duplicar un archivo ("archivo 2.tsx"). Se acumularon
# 107 sueltas en el repo; son basura, nunca fuente. Verificado antes de
# borrarlas: 106 idénticas byte a byte a su original y la restante un reporte
# de coverage generado.
*[ ]2.*
